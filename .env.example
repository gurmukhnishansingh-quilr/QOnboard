# ── Jira ──────────────────────────────────────────────────────────────────────
JIRA_URL=https://your-org.atlassian.net
JIRA_USERNAME=your@email.com
JIRA_API_TOKEN=your_jira_api_token

# Issue type name exactly as it appears in Jira
JIRA_ISSUE_TYPE=Customer Onboard

# Status / transition names for the "Customer Onboard" issue type workflow:
#   To Do  ──(New Tenant)──►  Tenant Provisioning  ──►  …  ──►  Tenant Ready
JIRA_PENDING_STATUS=To Do
JIRA_IN_PROGRESS_STATUS=New Tenant
JIRA_DONE_STATUS=Tenant Ready

# Custom field ID for the environment selector on the issue.
# Find it via: GET /rest/api/3/field  (look for "customfield_XXXXX")
# firstname / lastname / email are extracted from the description by the LLM.
JIRA_FIELD_ENVIRONMENT=customfield_10103

# ── LLM (Azure OpenAI) ───────────────────────────────────────────────────────
AZURE_OPENAI_API_KEY=your_azure_openai_key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_API_VERSION=2024-02-01

# ── Onboard API ───────────────────────────────────────────────────────────────
# Vendor value sent in the onboard payload (default: microsoft)
ONBOARD_VENDOR=microsoft
API_TIMEOUT_SECONDS=30

# ── Per-environment databases ─────────────────────────────────────────────────
# PostgreSQL and Neo4j credentials live in envs/.env.<env-name>
# Copy each .example file and fill in the real values:
#   envs/.env.uae-poc   ← UAE POC  (trust.quilr.ai)
#   envs/.env.ind-poc   ← IND POC  (platform.quilr.ai)
#   envs/.env.usa-poc   ← USA POC  (app.quilr.ai)
#   envs/.env.ind-prod  ← IND Prod (platform.quilrai.com)
#   envs/.env.usa-prod  ← USA Prod (app.quilrai.com)
